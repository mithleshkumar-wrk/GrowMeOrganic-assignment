# GrowMeOrganic Private Limited — Artworks Browser

A small React + TypeScript + Vite app that lists artworks from the Art Institute of Chicago API with paging, selection, and simple persistence.

Key features
- Browse artworks with pagination.
- Select rows across pages and persist selection to localStorage.
- Simple, responsive UI using PrimeReact components and TailwindCSS.
- Animated pages with Framer Motion.

Quick start
1. Install dependencies:
```sh
npm install
```
